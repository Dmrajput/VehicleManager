# Server
PORT=5000
NODE_ENV=development

# Database
MONGO_URI=mongodb://127.0.0.1:27017/vehicle_manager

# Auth
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=30d

# OTP (dev mode returns OTP in API response; integrate an SMS provider for prod)
OTP_DEV_MODE=true
OTP_EXPIRY_MINUTES=5
